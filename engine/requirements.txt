# Django and REST Framework
Django==5.2
djangorestframework==3.15.2
django-cors-headers==4.3.1
python-decouple==3.8

# Database
psycopg2-binary==2.9.9

# AI/ML Libraries
openai==1.3.0
sentence-transformers==3.0.1
scikit-learn==1.4.2
numpy==1.26.4
scipy==1.11.4
torch==2.3.1
torchvision==0.18.1

# Google Gemini
# Gemini SDK stack - keep these three in step across backend AND engine.
# proto-plus < 1.26 caps at protobuf < 5 and blows up at call time against a
# newer protobuf with a MessageMapContainer AttributeError that names neither
# package. Verified working together on 2026-09-17.
google-generativeai==0.8.4
proto-plus==1.28.4
protobuf==7.36.0
# Unified SDK - required only for GEMINI_BACKEND=vertex (Vertex AI path).
# Matches the version already pinned in backend/requirements.txt.
google-genai==1.39.0

# Perplexity AI

# Text Processing
textblob==0.17.1
nltk==3.8.1

# Task Queue
celery==5.3.4
redis==5.0.1

# HTTP and API
requests>=2.31.0
httpx>=0.27.0

# Additional ML Dependencies
huggingface-hub==0.24.2
pillow==10.4.0
regex==2024.11.6
safetensors==0.4.3
tokenizers==0.19.1
MarkupSafe==2.1.5
mpmath==1.3.0
networkx==3.3
sentencepiece==0.2.0
sympy==1.12
threadpoolctl==3.4.0
typer-slim==0.12.4
filelock==3.15.4
anyio==3.7.1
hf-xet==1.1.10

# Laminar (lmnr) observability — auto-instruments LLM SDK calls (OpenTelemetry).
lmnr[all]>=0.7.42,<0.8.0
# lmnr's bundled OpenAI instrumentor imports opentelemetry._events, which was
# REMOVED in opentelemetry 1.44. Pin the whole OTel stack to the 1.43 line (has
# _events) so ChatGPT/Grok/DeepSeek/Perplexity/Vertex-Gemini auto-trace. Keep
# these together — mixing 1.43/1.44 breaks the OTLP exporter.
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-semantic-conventions==0.64b0
opentelemetry-instrumentation==0.64b0
opentelemetry-instrumentation-threading==0.64b0
opentelemetry-exporter-otlp-proto-grpc==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-exporter-otlp-proto-common==1.43.0
opentelemetry-proto==1.43.0

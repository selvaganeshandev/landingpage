# ===================================
# Environment & Secrets
# ===================================
.env
.env.local
.env.*.local
*.env
!.env.example

# ===================================
# Python
# ===================================
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# The Python "lib/" and "dist/" rules above are meant for build output, but they
# also swallow the frontend's own source directories. That is how
# frontend/src/lib/platform-marks.ts came to be committed-against but absent
# from the repo, breaking the build everywhere except the machine that wrote it.
!frontend/src/lib/
!frontend/src/lib/**

# Virtual environments
venv/
env/
ENV/
.venv/
backend/.venv/
engine/.venv/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
staticfiles/
media/

# Flask
instance/
.webassets-cache

# Celery
celerybeat-schedule*

# Database dumps
*.rdb
dump.rdb
*.dump

# ===================================
# Node.js / Frontend
# ===================================
# Logs
logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules/

# Build outputs
dist/
dist-ssr/
*.local
build/
.next/
out/

# Testing
coverage/
.nyc_output/

# ===================================
# Editor & IDE
# ===================================
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo
*.swn
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ===================================
# OS
# ===================================
.DS_Store
Thumbs.db
*.bak
*.tmp

# Celery beat runtime state — regenerated on each start, never commit.
engine/celerybeat-schedule
engine/celerybeat-schedule.*